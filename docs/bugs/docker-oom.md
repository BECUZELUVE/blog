# 踩坑：Docker容器运行大模型OOM崩溃

## 现象
docker内部跑7B模型，运行一段时间直接被杀掉，报错OOM。

## 排查过程
1. 一开始以为宿主机内存不足，实际内存够用
2. 查看docker容器内存限制，容器默认设置内存上限过低
3. 大模型加载+上下文占用内存随对话累积

## 解决
1. 启动容器增加 `--memory` 参数放开容器内存限制
2. 对话开启上下文窗口截断，不要无限累积历史
3. 使用模型量化版本降低显存内存占用

## 经验
在容器中部署LLM，一定要确认容器资源限制。
